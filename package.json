{
  "name": "tanjungduriat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node -r dotenv/config bin/www",
    "develop": "nodemon -r dotenv/config bin/www",
    "create": "sequelize db:create",
    "drop": "sequelize db:drop",
    "migrate": "sequelize db:migrate",
    "rollback": "sequelize db:migrate:undo",
    "seed": "sequelize db:seed:all",
    "deploy": "yarn drop && yarn create && yarn migrate",
    "jest": "jest",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "node-fetch": "^3.3.0",
    "nodemon": "^2.0.20",
    "pg": "^8.8.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.25.5",
    "sequelize-cli": "^6.5.2"
  },
  "devDependencies": {
    "jest": "^29.3.1"
  }
}
